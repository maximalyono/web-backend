dist: focal
language: go
go:
- 1.14.x

install:
  - sudo apt-get install flameshot -y

jobs:
  include:
    - stage: "builder"
      name: "build dong"
      install: skip
      script: 
        - go build
    - script:
        - go test
      name: "build lagi"
    - stage: "deployment"
      name: "deploy aja"
      script:
