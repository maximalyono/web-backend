dist: focal
language: go

go:
- 1.14.x

jobs:
  include:
    - stage: "builder"
      name: "build dong"
      script: 
        - go build
    - script:
        - go test
      name: "build lagi"
    - stage: "deployment"
      name: "deploy aja"
      script:
